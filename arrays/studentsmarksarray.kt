fun main()
{
    val marks=Array(5) {0}
    val i=0
    for(i in 0..marks.size)
    {
        marks[i]=readln()?.toIntOrNull()?:0


    }
    for(i in 0..marks.size)
    {
        marks[i]=readln()?.toIntOrNull()?:0
        

    } 
}