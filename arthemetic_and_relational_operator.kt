fun main()
{
    var i=23
    var j=25
    println(i/j)//as both the i and j are the integer type variable or data type so it will give output of integer the integral part of 0.92 is 0 but if we need float output what we will do it to convert it explicitly
    println(i/j.toFloat()) //explicitly converted to float
    println(i*j)
    println(i+j)
    println(i-j)
    println(i%j) //it will give the remainder as it is modulo function as 23 divide be 25 will be not possible as for long division a>b so remainder be the number itself as we use 25 0 times
    

}